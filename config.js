
const botConfig = {
   token: "TOKEN",
   clientId: "852557080518852678",
   globalCommands: true,
   guilds: ["888876166570713108"], // You only need this if you want to register guild cmds instead of global ones
   maxRamUsage: 1024, //MB
   scheduledRestart: 0 // 0 = disabled, hours between restarts
}

module.exports = { botConfig }
